<Window
    x:Class="_48.plane.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:UserC="clr-namespace:_48.plane.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:_48.plane"
    Title="MainWindow"
    Width="800"
    Height="450">
    <Grid>
        <TabControl
            x:Name="TabCon"
            ItemsSource="{Binding .}"
            SelectionChanged="TabCon_SelectionChanged">
            <TabItem Header="{Binding Title}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <!--  第一列历史数据  -->
                    <UserC:LotteryTabControl Title="1231" />
                    <TextBlock Grid.Column="1" Text="{Binding Title}" />
                    <TextBlock Grid.Column="2" Text="xxx" />
                </Grid>
            </TabItem>
            <!--<TabControl.ItemTemplate>
                <DataTemplate>

                </DataTemplate>
            </TabControl.ItemTemplate>-->
        </TabControl>

    </Grid>
</Window>
